const express = require("express");
const router = express.Router();

// const { getMainNew, getSubBest, getSubDiscount } = require("../controller/pages");

// 메인페이지 신상품
// router.post("/main/new", getMainNew);

// 서브페이지 베스트
// router.post("/sub/best", getSubBest);

// 서브페이지 알뜰상품
// router.post("/sub/discount", getSubDiscount);

module.exports = router;
